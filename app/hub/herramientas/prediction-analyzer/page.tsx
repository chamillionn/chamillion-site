import PredictionAnalyzerClient from "./prediction-analyzer-client";

export const metadata = { title: "Prediction Analyzer" };

export default function PredictionAnalyzerPage() {
  return <PredictionAnalyzerClient />;
}
